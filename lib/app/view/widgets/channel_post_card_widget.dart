import 'package:flutter/material.dart';
import 'package:vehicleservicingapp/app/data/model/post.dart';

class ChannelPostCardWidget extends StatelessWidget {
  final Post post;
  const ChannelPostCardWidget({Key key, this.post}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card(
      child: Column(
        children: [],
      ),
    );
  }
}
